Em `src/main.tsx`, remover (comentar) o `<Guarantee />` para ocultar o bloco "7 Dias de Garantia" da página.

Nenhum outro arquivo será alterado. O componente `Guarantee.tsx` permanece no projeto caso queira reativar depois.